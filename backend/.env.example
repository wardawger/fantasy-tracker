PORT=20129

# Optional: if set, storage uses a persistent Turso database instead of a
# local sql.js file (needed for production, since Railway's default plan
# wipes local files on every deploy).
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
